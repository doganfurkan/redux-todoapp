import React from "react";

export default function Filters() {
  return (
    <div id="filters">
      <div id="buttons">
      <button>All</button>
      <button>Active</button>
      <button>Completed</button>
      </div>
    </div>
  );
}
